<%= form_with(model: [law, draft], local: true) do |form| %>
  <% if draft.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(draft.errors.count, "error") %> prohibited this draft from being saved:</h2>

      <ul>
      <% draft.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :published_at %>
    <%= form.datetime_select :published_at, id: :draft_published_at %>
  </div>

  <%= form.fields_for :document do |df| %>
  <div class="field">
    <%= df.label :source_url %>
    <%= df.text_field :source_url %>
  </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
